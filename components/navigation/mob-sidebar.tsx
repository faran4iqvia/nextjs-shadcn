import TestSidebarContent from './test-sidebar-content';

const MobSidebar = () => {
  return (
    <nav className="w-full h-full z-30 flex flex-col">
      <TestSidebarContent />
    </nav>
  );
};

export default MobSidebar;
